# This configuration file is for internal use only, for publishing key portions
# of this repo to our internal GitHub Pages instance

exclude_patterns = ['**/_build']

source_suffix = {
    '.rst': 'restructuredtext'
}

highlight_language = 'none'
text_newlines = 'unix'
